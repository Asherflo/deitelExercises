package ChapterFive;

public class ForControl {
    public static void main(String[] args) {
        for (int counter = 1; counter <= 10; counter++){ //for statement iterate a statement in a single line of code,
        // this line of code contain control variable(counter=1), loop contination condition(counter<=10) and
        // the increment condition of the control variable modified each time through the loop)    }
        System.out.println("" + counter);
    }
        System.out.println();

}
}
